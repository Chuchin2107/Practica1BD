services:
  postgres:
    image: postgres:16
    container_name: practica1-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: practica1
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
